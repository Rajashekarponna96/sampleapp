import { ReplaceZeroWithEmptyPipe } from './replace-zero-with-empty.pipe';

describe('ReplaceZeroWithEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceZeroWithEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
