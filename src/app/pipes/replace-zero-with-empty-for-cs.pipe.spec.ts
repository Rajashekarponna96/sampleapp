import { ReplaceZeroWithEmptyForCSPipe } from './replace-zero-with-empty-for-cs.pipe';

describe('ReplaceZeroWithEmptyForCSPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceZeroWithEmptyForCSPipe();
    expect(pipe).toBeTruthy();
  });
});
