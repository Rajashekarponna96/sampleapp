import { PersonWithoutEndpoint } from './person-without-endpoint';

describe('PersonWithoutEndpoint', () => {
  it('should create an instance', () => {
    expect(new PersonWithoutEndpoint()).toBeTruthy();
  });
});
